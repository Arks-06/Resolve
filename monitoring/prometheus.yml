global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'fastapi_gateway'
    static_configs:
      # host.docker.internal resolves to the host machine running the FastAPI gateway
      - targets: ['host.docker.internal:8000']